export const name = 'mockoon-config-transformer';
export const version = '2.0.0-beta.1';

export const SUPPORTED_MIGRATION = 28;
export const SUPPORTED_COMMANDS = {
  bundle: 'bundle',
  extract: 'extract'
};

export const enum SUPPORTED_OUTPUT_TYPES {
  json = 'json'
}

export const DOCUMENTATION_FILE_NAME = 'documentation';
export const METHOD_DIR_TEMPLATE = '___method';
export const RESPONSES_DIR_NAME = '___responses';

export const MAX_DIRECTORY_NAME_LENGTH = 200;

export const MAX_DOCUMENTATION_DIRECTORY_LENGTH = 30;
